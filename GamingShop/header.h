#pragma once
#include <iostream>
#include <exception>
#include <fstream>
#include <string>
#include <list>
#include <stdlib.h>
using namespace std;

bool num_valid(string str);
bool str_valid(string str);